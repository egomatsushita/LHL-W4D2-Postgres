{
  "name": "postgres",
  "version": "1.0.0",
  "description": "Connect a PostgreeSQL database form JS directly using the native pg driver.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "postgreSQL"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "knex": "^0.13.0",
    "pg": "^7.0.0"
  }
}
